{
  "name": "boxitlogistics-site",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i tailwind-input.css -o assets/tailwind.css --minify",
    "build:html": "node scripts/build-static-html.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
